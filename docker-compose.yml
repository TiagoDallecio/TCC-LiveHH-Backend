version: '3.8'
services:
  postgres-livehh:
    image: postgres:16-alpine
    container_name: postgres-livehh
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: livehh_db
    ports:
      - "5432:5432"
    restart: unless-stopped